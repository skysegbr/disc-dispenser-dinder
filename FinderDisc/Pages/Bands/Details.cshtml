@page
@model FinderDisc.Pages.Bands.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Band</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Band.BandName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Band.BandName)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Band.BandId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
