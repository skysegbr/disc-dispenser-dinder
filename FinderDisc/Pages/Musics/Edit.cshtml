@page
@model FinderDisc.Pages.Musics.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Music</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Music.MusicId" />
            <div class="form-group">
                <label asp-for="Music.MusicName" class="control-label"></label>
                <input asp-for="Music.MusicName" class="form-control" />
                <span asp-validation-for="Music.MusicName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Music.BandId" class="control-label"></label>
                <select asp-for="Music.BandId" class="form-control" asp-items="ViewBag.BandId"></select>
                <span asp-validation-for="Music.BandId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
