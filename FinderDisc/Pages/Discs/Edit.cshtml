@page
@model FinderDisc.Pages.Discs.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Disc</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Disc.DiscId" />
            <div class="form-group">
                <label asp-for="Disc.DiscName" class="control-label"></label>
                <input asp-for="Disc.DiscName" class="form-control" />
                <span asp-validation-for="Disc.DiscName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Disc.MusicId" class="control-label"></label>
                <select asp-for="Disc.MusicId" class="form-control" asp-items="ViewBag.MusicId"></select>
                <span asp-validation-for="Disc.MusicId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Disc.DispenserId" class="control-label"></label>
                <select asp-for="Disc.DispenserId" class="form-control" asp-items="ViewBag.DispenserId"></select>
                <span asp-validation-for="Disc.DispenserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
